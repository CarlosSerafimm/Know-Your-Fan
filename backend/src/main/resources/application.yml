spring:
  application:
    name: backend
  datasource:
#    url: jdbc:mysql://mysql-furia:3306/furia
    url: jdbc:mysql://localhost:3307/furia
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    #    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    #      ddl-auto: none
    properties:
      hibernate.format_sql: true